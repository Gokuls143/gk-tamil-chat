# Production Database Configuration for Railway
spring.datasource.url=${DATABASE_URL:jdbc:mysql://mysql-5llz.railway.internal:3306/railway?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Kolkata&autocommit=false&rewriteBatchedStatements=true&cachePrepStmts=true&useServerPrepStmts=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:ChxllcuozQmOcUIhVbijctZVhpVgrdwn}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Timezone Configuration - Set to Indian Standard Time
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata

# JPA Configuration - Optimized for Railway
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.connection.autocommit=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Railway Database Connection Pool Optimization
spring.datasource.hikari.maximum-pool-size=8
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=false
spring.datasource.hikari.leak-detection-threshold=60000

# Server Configuration
server.port=${PORT:8080}

# Session Configuration for Railway
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# File Upload Configuration - Railway optimized
spring.servlet.multipart.max-file-size=8MB
spring.servlet.multipart.max-request-size=8MB
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=1MB
spring.servlet.multipart.resolve-lazily=true

# Railway Performance Optimizations
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

# Railway Static Resource Caching
spring.web.resources.cache.period=3600
spring.web.resources.chain.strategy.content.enabled=false
spring.web.resources.chain.strategy.fixed.enabled=false

# Logging for Production
logging.level.root=INFO
logging.level.com.example.demo=INFO
logging.level.org.springframework.web.socket=WARN
logging.level.org.springframework.messaging=WARN
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Kolkata
